# Stage 7 U3 B2 A1 Increment 9L state-based inspection correction

## Status

`WORKFLOW / EVIDENCE-INSPECTION CORRECTION ONLY`

This record corrects an absolute solver-step assumption in the Increment 9L evidence inspection. It does not change the physics models, root construction, boundary state machine, FvmSolver trajectory, accepted tolerances, or locked contracts.

## Failed workflow authority

```text
source Git SHA:
bdbbf88b240ff1b839d8a72fa898437efac1e7b8

workflow run:
31690390529

job:
94416115006

artifact:
9177683047

artifact name:
u3-b2-a1-increment-9l-v5-31690390529

artifact SHA256:
0ff366738c855c83d9355c3e18b2cb54f640354a34b96211d85dd205269f6b32
```

The workflow job conclusion was `failure`, but the actual Increment 9L runner step and the final runner-success requirement both concluded `success`. The inspection step alone failed.

## Successful engineering trajectory retained by the failed workflow

```text
initial state used: true
checkpoint artifact used: false
single FvmSolver instance: true
accepted steps: 640
final solver step: 640
final time: 0.004285834855172021 s
nominal 2L/c0: 0.004285834855172021 s
horizon error: 0.0 s
working vertical slice: true
```

The public boundary sequence was:

```text
OUTWARD_FLOW: 637 accepted steps
ZERO_TRANSFER_CLOSED: 3 accepted steps
public transition count: 1
public chatter: false
```

The transition event was generated by state classification, not by an absolute step condition:

```text
OUTWARD_FLOW -> ZERO_TRANSFER_CLOSED
trigger: NO_ADMISSIBLE_ISLAND
failed candidate used as root: false
failed candidate used as flux: false
solver state mutated before transition: false
absolute step-number trigger used: false
```

## Inspection defect

The failed inspection retained the historical checkpoint-continuation expectation:

```python
assert bounded['first_fallback_requested_step'] == 606
```

That expectation belonged to a continuation beginning at accepted step 605. Increment 9L instead starts at the locked initial state and executes the entire trajectory with one FvmSolver instance.

For the end-to-end trajectory, the actual internal model progression was:

```text
CONNECTED_RAREFACTION
    -> GENERAL_THREE_BRANCH_CLASSIFICATION
    -> GENERAL_EOS_FINITE_COMPRESSION
    -> ZERO_TRANSFER_CLOSED
```

The finite-compression model transition occurred when the state classification first required it, and the bounded-window fallback became available from that model regime. Therefore an absolute expected first fallback step is not a valid reusable tool invariant.

## Corrected inspection rule

The clean rerun shall inspect causal and state-based ordering instead of a fixed solver step.

Required invariants:

```text
1. bounded-window fallback event steps are strictly increasing and unique;
2. correction summary first/last steps equal min/max event steps;
3. first fallback is not earlier than the GENERAL_EOS_FINITE_COMPRESSION transition;
4. last fallback is earlier than the OUTWARD_FLOW -> ZERO_TRANSFER_CLOSED transition;
5. every fallback accepted step remains in public state OUTWARD_FLOW;
6. every fallback accepted step uses GENERAL_EOS_FINITE_COMPRESSION;
7. every fallback event is triggered only by a predeclared seeded-edge classification;
8. every retained topology has one bounded success window, monotone root topology, one sign-change bracket, and a passing selected root;
9. no excluded or failed candidate is used as a root endpoint or flux state;
10. no checkpoint, absolute step trigger, or hard-coded transition time is used.
```

The workflow shall continue to require:

```text
initial-state start
single FvmSolver instance
one public boundary transition
NO_ADMISSIBLE_ISLAND closure trigger
zero mass / energy / vapor transfer after closure
wall-pressure momentum identity
finite conserved state
positive density and internal energy
single-phase liquid
rho*xv exact zero
step and cumulative conservation gates
actual 2L/c0 target reach
```

## Classification

```text
workflow / evidence-inspection bookkeeping defect
```

This is not:

```text
a solver failure
a physical-model failure
a root failure
a conservation failure
a positivity failure
a phase-scope departure
a branch-chatter event
```

## No-change boundary

The correction does not change:

```text
B1
Hugoniot equations
root or velocity tolerances
chi scope
scan-node counts
production adapter
FvmSolver core
locked B2 contract
zero-transfer closure flux
re-entry policy
reverse-flow policy
```

## Formal-state boundary

Even after a clean workflow success, the result remains:

```text
PROVISIONAL ENGINEERING END-TO-END WORKING SLICE
```

The following remain false:

```text
finite_compression_branch_approved
multi_step_finite_compression_continuation_authorized
full_two_l_over_c0_passed
formal_state_promoted
u3_b2_finite_pipe_execution_complete
single_phase_finite_pipe_coupling_verified
u3_b2_verification_benchmark_accepted
physical_validation
design_use_acceptance
production_hem_activation_approved
```
