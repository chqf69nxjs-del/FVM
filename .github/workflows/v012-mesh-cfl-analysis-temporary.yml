name: V-012 Mesh CFL Analysis Temporary

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  synchronize-docs:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout work branch
        uses: actions/checkout@v7
        with:
          ref: agent/stage6-v012-mesh-cfl-observation
          fetch-depth: 0

      - name: Synchronize documentation
        run: python tools/sync_v012_mesh_cfl_docs.py

      - name: Commit synchronized documentation
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add \
            docs/verification/MASTER_VERIFICATION_INDEX.md \
            docs/verification/stage6_execution_log.md \
            docs/verification/stage6_v012_execution_log.md
          git diff --cached --check
          if git diff --cached --quiet; then
            echo "No documentation changes to commit"
            exit 0
          fi
          git commit -m "Record V-012 mesh/CFL observation"
          git push origin HEAD:agent/stage6-v012-mesh-cfl-observation
